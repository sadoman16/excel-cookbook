---
title: "VLOOKUP 완전 정복: #N/A 오류부터 정확한 검색까지"
description: "VLOOKUP 함수의 모든 것 - 기본 사용법, 흔한 오류 해결, 그리고 프로 팁까지"
date: "2025-01-15"
tags: ["VLOOKUP", "Lookup & Reference", "오류해결"]
---

# VLOOKUP 완전 정복 🔍

## 문제 상황 (The Problem)

"두 시트에 있는 데이터를 비교해서 일치하는 값을 찾고 싶은데... 어떻게 하지?"

이런 상황, 한 번쯤 겪어보셨죠? **VLOOKUP**이 바로 이 문제의 해결사입니다.

## 재료 준비 (The Ingredients)

```
=VLOOKUP(lookup_value, table_array, col_index_num, [range_lookup])
```

| 매개변수 | 설명 |
|---|---|
| `lookup_value` | 찾고 싶은 값 |
| `table_array` | 검색할 범위 |
| `col_index_num` | 반환할 열 번호 |
| `[range_lookup]` | TRUE(근사값) / FALSE(정확한 값) |

## 레시피 (Step-by-Step)

### Step 1: 데이터 준비
A열에 직원 이름, B열에 부서, C열에 급여가 있다고 가정합니다.

### Step 2: 수식 입력
```
=VLOOKUP("김철수", A2:C10, 3, FALSE)
```

### Step 3: 결과 확인
"김철수"의 급여가 반환됩니다!

## 프로 팁 (Pro Tips) 💡

> **Always use FALSE (0) for exact match** unless the data is sorted and you specifically need approximate match.

## 자주 발생하는 오류 🚨

- **#N/A (Not Found)**: 찾는 값이 범위에 없을 때
- **#REF! (Invalid Column Index)**: 열 번호가 범위를 초과할 때
- **#VALUE! (Wrong Data Type)**: 데이터 타입이 맞지 않을 때
